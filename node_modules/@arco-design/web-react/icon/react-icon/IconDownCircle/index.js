import _extends from "@babel/runtime/helpers/extends";
import React, { useContext } from 'react';
import { IconContext } from '../context';

function IconDownCircleComponent(iconProps, ref) {
  const _useContext = useContext(IconContext),
        _useContext$prefixCls = _useContext.prefixCls,
        prefixCls = _useContext$prefixCls === void 0 ? 'arco' : _useContext$prefixCls;

  const spin = iconProps.spin,
        className = iconProps.className;
  const props = {
    "aria-hidden": true,
    focusable: false,
    ref,
    ...iconProps,
    className: `${className ? className + ' ' : ''}${prefixCls}-icon ${prefixCls}-icon-down-circle`
  };

  if (spin) {
    props.className = `${props.className} ${prefixCls}-icon-loading`;
  }

  delete props.spin;
  delete props.isIcon;
  return /*#__PURE__*/React.createElement("svg", _extends({
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "4",
    viewBox: "0 0 48 48"
  }, props), /*#__PURE__*/React.createElement("circle", {
    cx: "24",
    cy: "24",
    r: "18",
    transform: "rotate(-180 24 24)"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M32.484 20.515 24 29l-8.485-8.485"
  }));
}

const IconDownCircle = /*#__PURE__*/React.forwardRef(IconDownCircleComponent);
IconDownCircle.defaultProps = {
  isIcon: true
};
IconDownCircle.displayName = 'IconDownCircle';
export default IconDownCircle;